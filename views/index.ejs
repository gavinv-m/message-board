<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Message Board</title>
  </head>
  <body>
    <h1>Message Board</h1>
    <a href="/new">Add a new message</a>
    <div>
      <% messages.map((message) => {%>
      <h2><%= message.username %></h2>
      <p><%= message.text %></p>
      <h6>Posted on: <%= message.added %></h6>
      <% }) %>
    </div>
  </body>
</html>
